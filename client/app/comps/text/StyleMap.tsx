import { getRadioUtilityClass } from "@mui/material";

const styleMap = {
    'CODE': {
        color: "red",
        "fontFamily": "monospace, monospace",
        // background: "#eee",
        borderRadius: "5px",
        paddingLeft: "0.5rem",
        paddingRight: "auto",
    }
};

export default styleMap;